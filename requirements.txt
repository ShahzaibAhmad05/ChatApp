# requirements.txt for python-chat-app
# This project currently uses only the Python standard library (no third-party packages).
# If you add external dependencies, list them here, one per line. Example:
# requests==2.31.0

# No packages to install at the moment.
